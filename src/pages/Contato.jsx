import React from 'react';

function Contato() {
  const pageStyle = {
    padding: '20px',
    textAlign: 'center',
  };

  return (
    <div style={pageStyle}>
      <h1>Contato Page</h1>
    </div>
  );
}

export default Contato;